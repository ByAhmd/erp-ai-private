export enum AccountType {
  Asset = 'Asset',
  Liability = 'Liability',
  Equity = 'Equity',
  Revenue = 'Revenue',
  Expense = 'Expense',
}

export enum JournalEntryStatus {
  Draft = 'Draft',
  Posted = 'Posted',
}

export enum AccountingPeriodStatus {
  Open = 'Open',
  Closed = 'Closed',
}
