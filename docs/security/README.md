# Security

This skeleton includes early security structure only.

Future work must add authentication, password policy, tenant membership enforcement, role and permission checks, audit logging, rate limiting, secrets management, and production CORS configuration.

No secrets should be committed. Start from `.env.example` and keep real values in `.env`.
